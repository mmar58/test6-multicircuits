/**
 * Shared gate rendering constants.
 * Used by GateElement.svelte, WireLayer.svelte, and CircuitCanvas.svelte
 * to ensure pin positions are always consistent.
 */
export const GATE_WIDTH = 80;
export const GATE_HEIGHT = 50;
